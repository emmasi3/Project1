#include <iostream>

int main()
{
#if defined __GNUC__ || defined __llvm__
	/// LIKCLY 宏的封装, 告诉编译器优化,条件大概率成立
#   define SYLAR_LIKELY(x)       __builtin_expect(!!(x), 1)
/// LIKCLY 宏的封装, 告诉编译器优化,条件大概率不成立
#   define SYLAR_UNLIKELY(x)     __builtin_expect(!!(x), 0)
#else
#   define SYLAR_LIKELY(x)      (x)
#   define SYLAR_UNLIKELY(x)      (x)
#endif
	/*
	* 以上的宏定义，是在 sylar -- 服务器项目中学习到的：
	* 目的是为了对于某些 大概率条件减少汇编层面的判断资源消耗
	* 
	* 这是我对 chat-gpt 表达的思路、观点：
	* 所以我来梳理一遍他实现的具体原理，你看我理解的对不对？如果不对，请指出错误，并提出修改建议:
	* 
		*1、仅仅使用C++提供的基本工具，无法实现从汇编码的执行路径优化，也就是无法做到不浪费资源；
		*2、那么使用了某些编译器提供的特殊方法之后，就会影响到汇编码生成的方式，
		*从汇编码选择路径的角度去使得大概率的时间只需要执行一次，
		*而不是像if-else要额外执行2次或者多次，显著减少了资源消耗是吧

		1 的观点 -- ❌ 这句话 不完全正确
		哪里不对？
		C++ 本身并不禁止这种优化
		现代编译器在“没有任何提示”的情况下，也会做分支预测和路径布局
		也就是说：
		❌ 不是 “无法优化”
		✅ 而是 “编译器只能猜，而不是确定”

	* 
	* ✅ 最终修正后的完整理解（推荐作为笔记）

	1、在标准 C++ 语法层面，程序只能表达“条件是否成立”，而无法表达“某个条件发生的概率”；因此，
	在没有额外信息的情况下，编译器只能基于启发式规则对分支进行推测性优化，
	而无法确定哪一条执行路径是高频路径。（这句话是为了纠正我的第 1 点，）

	2、通过使用编译器提供的分支预测提示（如 __builtin_expect 封装的 LIKELY / UNLIKELY 宏），
	程序员可以显式告知编译器某个条件更可能为真或为假，从而影响编译器生成汇编代码时的分支布局，
	使高概率分支成为顺序执行或无跳转路径，低概率分支成为跳转路径。

	3、这种优化并不会减少 if / else 本身的判断次数，也不会改变程序的逻辑语义；
	它优化的核心目标是 “降低 CPU 分支预测失败的概率，减少由于流水线回滚、指令重取和缓存失效带来的性能损耗”。

	4、在包含多个 if / else if / else 的条件链中，程序在一次执行路径上会按顺序对条件进行判断，
	一旦命中某个分支就立即跳转执行该分支，其后的条件将不会再被判断；
	因此，编译器优化的重点不在于减少“源码中条件的数量”，而在于保证最常被命中的分支尽可能以最小代价执行。

	5、LIKELY / UNLIKELY 的价值主要体现在分支冷热极不对称的场景（如错误处理、异常路径、资源耗尽等），
	在这些场景下，它能够显著提升 CPU 的指令流水效率，但在分支概率接近的情况下使用，反而可能造成负优化。
	* 
	*/










	return 0;
}