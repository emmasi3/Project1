/* 
* 服务器学习 -- 项目 -- config.h中的问题
* 
* 3、还遇到一个问题，我在 config.h 文件中，（2行代码）同时实现了 s_datas 的声明和实现，很正常吧？
* 但是当我在 config.cpp 中 #include "config.h"，仅仅只有这一句时，报错了，显示的是：
* s_datas 重定义？
*	原因：编译时，假设有 3个文件中包含了 config.h，那么就会生成 3份 s_datas 的定义，在链接阶段，一定会报错 -- 重复定义
*		但是如果包含的.h 中仅仅只有 s_datas 的声明的话，无论被包含多少次，都不会报错，因为.cpp 中的定义，从始至终
*		都只被编译了一次，不会导致重复定义
*	那么这时候你就会产生疑问：为什么我之前在类中定义的很多变量和成员函数的实现，都在log.h中，为什么被包含了很多次
*	都不会显示重复定义呢？
*
*| 特性 | 链接层面 | 内存分配 |
*| ------------ - | ---------- | ---------------------------- - |
*| 非静态成员变量，类内初始化 | 不生成符号 | 每个对象有自己的内存 |
*| 静态成员变量，类内初始化 | 生成符号（全局共享） | 共享一份内存，需要 `inline static` 才安全 |
*分两种情况：
* 1）变量：
*		除了静态(成员)变量 —— 全局变量 ——外，其余变量（类成员、普通）都不会在编译阶段生成“符号”，
*		之所以有符号生成，是因为共享内存的缘故，自己问AI
* 2）普通函数：
*		如果是普通函数（非类成员函数、非constexpr函数、非模版函数），那么编译器会对函数生成“符号”，可以预见，如果每一个包含该.h的cpp
*		都编译一遍，生成“符号”，肯定会重定义的，事实也是如此
* 3）成员函数、模板函数、constexpr函数：
*		模板函数、constexpr函数都会隐式地被 inline修饰，所以链接阶段，所有的“符号”会被合并，多cpp包含安全
*		成员函数，如果是 getValue() { return m_value; } 这种声明实现一体的，会被inline修饰，多cpp包含安全
*				如果是在同一个文件中，类内声明，类外定义，不会被 inline 修饰，多cpp包含会报错，符号没有合并
*				.h声明，.cpp定义，没问题，设计本身就是 -- 多cpp包含安全
* 
* 所以你能看见，无论是 静态变量 || 函数，出现多cpp包含问题的本质，都是重复定义？如何判断重复定义呢？
* inline ，编译器生成的“符号”，在被多次编译的情况下，是否能够“合并”呢？如果是同名同类型对象，为何会有多个符号呢？
* 这就是评判标准
* 而外在的表现形式：1）inline 修饰
*				   2）遵循 .h 声明，.cpp 实现的设计思路
* 
* 从 C++17 开始 inline static 可以用来直接创建 静态成员变量，不用自己去实现类内声明，类外定义，编译器会自动生成
* 
* 下面给出一个表格，该表格分析了C++中全部类型的变量，关于是否在编译阶段生成“符号”的问题：
* | 变量类型                    | 是否生成符号       | 链接器行为                    | 头文件多次包含安全？ | 备注                           |
| ----------------------- | ------------ | ------------------------ | ---------- | ---------------------------- |
| **普通全局/命名空间变量**         | ?            | 链接器把每个翻译单元的符号合并 → 多重定义报错 | ?          | C++17+ 可加 `inline` 安全        |
| **类静态成员变量**			      | ?            | 链接器把每个翻译单元的符号合并 → 多重定义报错 | ?          | C++17+ 可加 `inline static` 安全 |
| **普通非静态成员变量**            | ?            | 每个对象有自己的内存，不生成全局符号       | ?          | 不涉及链接                        |
| **局部变量（函数内部）**          | ?            | 存放在栈或寄存器                 | ?          | 每次调用独立，不生成符号                 |
| **constexpr 变量**                | ? / 取决于使用场景  | 编译期求值，可能不生成符号            | ?          | 隐式 inline，可放头文件              |
| **inline 全局变量**               | ?（标记 inline） | 链接器合并符号                  | ?          | C++17+ 可以放头文件                |
| **模板静态成员变量**              | ?（实例化时生成）    | 链接器合并                    | ?          | 多个 cpp 包含安全，只生成需要实例化         |
| **枚举常量 / constexpr 枚举**     | ?            | 编译期常量                    | ?          | 不生成符号                        |

* 
* 自己回忆吧！
* 
* 4）生不生成“符号”，主要看该变量、函数，是否具有链接属性，那么在“全局变量和全局静态变量”这个文件中，提到的：
*	关于：static + 全局变量是为了 不被外部文件访问的讨论。
*	那么既然给“全局变量”+ static 是为了不被外部文件访问，那么编译阶段，是否还会生成“符号”用来链接呢？
*	回答：1）依旧会生成符号，只不过符号有很多类型，在这里是 -- 内部链接符号，所以也不会被外部文件访问到
* 
*/